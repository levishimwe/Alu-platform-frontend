// === backend/services/notificationService.js ===
exports.sendNotification = async (userId, message) => {
  // Placeholder for push notifications or internal messaging
  console.log(`Notify User ${userId}: ${message}`);
};
